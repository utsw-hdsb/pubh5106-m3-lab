#!/bin/bash
# Binder post-build script
# Pre-download Gilda grounding database (~200 MB) so it's ready at launch
python -c "import gilda; gilda.ground('test')"

# Enable ipywidgets for JupyterLab (Binder default)
jupyter labextension install @jupyter-widgets/jupyterlab-manager 2>/dev/null || true
